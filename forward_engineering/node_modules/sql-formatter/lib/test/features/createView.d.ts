import { FormatFn } from "../../src/sqlFormatter";
interface CreateViewConfig {
    orReplace?: boolean;
    materialized?: boolean;
}
export default function supportsCreateView(format: FormatFn, { orReplace, materialized }?: CreateViewConfig): void;
export {};
